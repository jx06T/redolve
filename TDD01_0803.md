# Redolve 技術設計文件 (Technical Design Document)

## 1. 系統概述 (System Overview)

Redolve 是一款專為 iPad + Apple Pencil 深度最佳化的 Serverless PWA 錯題本系統。主打「心流式刷題（無縫滾動）」、「雙手協同手勢」與「AI 自動化標籤」。系統全面採用 Cloudflare 邊緣運算生態系，並導入標準 SaaS 多使用者驗證架構，實現跨裝置安全存取與資料完全隔離。

### 1.1 核心技術堆疊 (Tech Stack)

| 層級 | 技術選型 | 說明 / 備註 |
| :--- | :--- | :--- |
| **前端 (Frontend)** | React 18, Vite (SPA), TypeScript, Tailwind CSS, Zustand, `@tanstack/react-virtual` | 效能優化與虛擬列表 |
| **繪圖引擎 (Canvas)** | HTML5 原生 `<canvas>` + `perfect-freehand` | 支援像素級局部擦除 |
| **後端 API (Backend)** | Cloudflare Workers (Hono Framework) | Edge Runtime |
| **資料庫與儲存** | Cloudflare D1 (SQLite) + Cloudflare R2 | 關聯式資料與物件儲存 |
| **身分驗證 (Auth)** | better-auth (Google OAuth) + 自建 API Key Middleware | 雙軌驗證機制 |
| **AI 整合 (AI)** | Google Gemini 2.5 / 3.5 Flash API | Structured Outputs 視覺辨識 |

---

## 2. 身分驗證與安全架構 (Auth & Security)

系統採用**雙軌驗證機制 (Dual Auth Architecture)**，以服務不同的客戶端需求：

1. **PWA 網頁端 (人類操作)**：透過 Google OAuth 登入。前端依賴 Session / HTTP-only Cookies 進行驗證，確保 iPad Safari 上的存取安全。
2. **iOS 捷徑端 (機器自動化)**：使用靜態 API Key。使用者在 PWA 後台生成一組專屬 API Key，並填入 iOS 捷徑的 HTTP Header (`Authorization: Bearer <API_KEY>`) 中。
3. **資料隔離 (Multi-tenant Isolation)**：所有對 D1 資料庫的 CRUD 操作，皆須透過 Hono Middleware 攔截並解譯出 `user_id`，所有 SQL 查詢強制附加 `WHERE user_id = ?` 條件。

---

## 3. 資料庫綱要設計 (Database Schema - Cloudflare D1)

```sql
-- 使用者帳號表 (Google OAuth 寫入)
CREATE TABLE users (
    id TEXT PRIMARY KEY,               -- UUID
    email TEXT UNIQUE NOT NULL,
    name TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- API 授權金鑰表 (供 iOS 捷徑使用)
CREATE TABLE api_keys (
    key TEXT PRIMARY KEY,              -- 加上前綴的隨機字串 (ex: rdv_xxx)
    user_id TEXT NOT NULL,
    description TEXT,                  -- ex: "iPad 捷徑專用"
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- 錯題/筆記主表
CREATE TABLE items (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,             -- 多租戶資料隔離關鍵
    type TEXT NOT NULL,                -- 'problem' | 'note'
    subject TEXT,                      
    source TEXT,                       
    topic TEXT,                        
    keywords TEXT,                     -- JSON Array
    image_url TEXT NOT NULL,           
    draw_url TEXT,                     -- 透明筆跡圖層 URL
    status TEXT DEFAULT 'processing',  -- 'processing' | 'unsolved' | 'resolved'
    review_count INTEGER DEFAULT 0,    
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- 查詢效能最佳化索引
CREATE INDEX idx_user_subject_topic ON items(user_id, subject, topic);
CREATE INDEX idx_user_status ON items(user_id, status);

-- FTS5 全文檢索虛擬表 (Global Search)
CREATE VIRTUAL TABLE items_fts USING fts5(id UNINDEXED, user_id UNINDEXED, subject, source, topic, keywords);
```

---

## 4. API 路由設計 (Hono Router)

### 4.1 身分驗證模組 (`/api/auth/*` & `/api/keys`)
- `GET/POST /api/auth/*`：由 better-auth 接管，處理 Google 登入、登出與 Session 驗證。
- `POST /api/keys`：生成一組新的 API Key 寫入 D1 並回傳給前端。
- `GET /api/keys`：列出該使用者目前所有的 API Key (打碼顯示，如 `rdv_****`)。
- `DELETE /api/keys/:key`：撤銷特定 API Key。

### 4.2 錯題資源模組 (`/api/problems`)
> *附註：所有以下路由皆受 Auth Middleware 保護，自動從 Session 或 Header 提取 `user_id`*

- `POST /api/problems`：
  1. 接收 `FormData` (包含壓縮後的二進位圖片)。
  2. 寫入 R2 與 D1，回傳 200 OK (不阻塞)。
  3. 透過 `ctx.waitUntil()` 於背景呼叫 Gemini API 進行 AI 標籤辨識並非同步 UPDATE 資料庫。
- `GET /api/problems`：獲取題目列表。支援分頁 (limit/offset) 與條件過濾。
- `GET /api/problems/:id`：獲取單題完整 Metadata。
- `PUT /api/problems/:id`：手動修正 Metadata (科目、單元、標籤)。
- `DELETE /api/problems/:id`：刪除錯題與關聯的 R2 影像檔。

### 4.3 高頻狀態更新模組
- `PATCH /api/problems/:id/draw`：前端 Debounce (2s) 將 Canvas 轉 Blob 覆寫至 R2。
- `PATCH /api/problems/:id/status`：切換狀態 (`unsolved` <-> `resolved`)，並觸發 `review_count` 累加。

---

## 5. 前端架構與 UI/UX 規範 (Frontend & Mechanics)

### 5.1 路由設計 (React Router)
- `/`：Dashboard 戰情室 (個人進度統計、API Key 管理入口)。
- `/study/:subject`：單科全域虛擬滾動刷題區 (List View)。
- `/study/:subject/:topic`：單元過濾視圖。
- `/study/:subject/:topic/:problemId`：URL 滾動狀態綁定 (`history.replaceState`)，F5 重整不遺失閱讀進度。
- `/problem/:id`：單題專注模式。
- `/search?q={keyword}`：全站 FTS5 獨立搜尋頁面。
- `/share/:id?ink=false`：公開唯讀分享頁 (若無 Token，僅允許唯讀檢視)。

### 5.2 核心互動實作

#### 觸控分離 (PointerType Magic)
- 攔截 `PointerEvent`，若為 `pen` 則交由 Canvas 繪圖。
- 若為 `touch` 則設定 `pointer-events: none` 穿透畫布，觸發外層 `<QuestionCard>` 容器原生垂直滾動。

#### 雙手協同 (Bimanual Interaction)
- **左下角彈簧橡皮擦 (Spring-loaded Eraser)**：左手按住 `onTouchStart` 切換 `ctx.globalCompositeOperation = 'destination-out'` 實現像素級擦除；鬆開恢復畫筆。
- **雙指復原 (Two-finger Undo)**：監聽全局 `touchstart`，`e.touches.length === 2` 觸發狀態棧回退。

#### 虛擬滾動與畫布擴展
- 僅渲染 Viewport 內的 `<QuestionCard>` (含作用中的 Canvas 實例)。
- 追蹤最低 Y 座標，動態更新容器 `min-height` 實現向下無限擴展。

#### 智慧反色模式 (Invert Mode)
- 針對底圖套用 `filter: invert(1) hue-rotate(180deg)`，保護彩色圖表同時提供黑底白字護眼體驗。